let initialState = {
	crimeDate: null,
    crimes: []
}

export default (state = initialState, action) => {
    let newState

    switch (action.type) {

        default:
            return state
    }
}